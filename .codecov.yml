coverage:
  status:
    project:
      default:
        target: auto
        threshold: 1%
    patch:
      default:
        target: auto
        threshold: 1%

comment:
  layout: "header, hit, diff, flags, components"
  behavior: default
  require_changes: false  # Force a comment even if coverage is unchanged
